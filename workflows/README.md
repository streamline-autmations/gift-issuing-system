# Workflows

This folder is reserved for repeatable project workflows.

Suggested future contents:
- local setup checklist
- Supabase migration workflow
- deploy verification workflow
- print-server validation workflow

The original Trae snapshot did not include dedicated workflow definition files.

Current workflows:
- `printing-setup.md` - per-computer slip printer setup and startup commands
